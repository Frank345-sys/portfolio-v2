/**
 * Sección **Proyectos** (`SECTION_ANCHOR_ID.proyectos`): scroll sincronizado, **`ol`** de **`article`**,
 * panel lateral y rail en **`lg`**, modal de vista ampliada cuando aplica.
 *
 * Datos: `./constants/projects.ts`; landmarks: `./constants/landmarks.ts` (sin `constants/index.ts`). Hooks y UI en subcarpetas con imports directos.
 *
 * @module components/ProjectsSection
 * @remarks Importar desde este barrel: `import { ProjectsSection } from '@/components/ProjectsSection'`.
 */
export { ProjectsSection } from './ProjectsSection'
