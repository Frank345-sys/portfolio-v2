/**
 * Hero principal (`SECTION_ANCHOR_ID.inicio` en `@/shared/constants/sectionAnchors`).
 *
 * Landmark **`<section>`** + **`HERO_SECTION_TITLE_ID`** (`./constants.ts`). `BackgroundBoxes` como fondo;
 * datos en `./constants`; bloques en `./subcomponents`.
 *
 * @module components/HeroSection
 * @remarks Importar desde este barrel: `import { HeroSection } from '@/components/HeroSection'`.
 */
export { HeroSection } from './HeroSection'
