/**
 * Sección de contacto (`SECTION_ANCHOR_ID.contacto` en `@/shared/constants/sectionAnchors`).
 *
 * **Landmark:** `<section>` + `CONTACT_SECTION_TITLE_ID` (+ columna **`CONTACT_MAIN_COLUMN_HEADING_ID`**) en
 * `./ContactSection.tsx`. Subbloques en
 * `./subcomponents`; datos en `./constants`.
 *
 * @module components/ContactSection
 * @remarks Importar desde este barrel: `import { ContactSection } from '@/components/ContactSection'`.
 */
export { ContactSection } from './ContactSection'
