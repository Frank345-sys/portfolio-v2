/**
 * Tokens de marca: tamaños y utilidades compartidas entre cabecera y pie
 * (evita acoplar `Footer` a `Header/styles`).
 *
 * @module shared/constants/tokens/brand
 */
export const BRAND = {
  /** Tamaño del icono junto al nombre del sitio (logo en header y enlace de marca en footer). */
  logoIcon: 'h-7 w-7 md:h-10 md:w-10',
} as const
