/**
 * Barrel de `ImageCarousel`: re-exporta desde este directorio.
 *
 * @module shared/components/ImageCarousel
 * @remarks Importar desde este barrel: `import { … } from '@/shared/components/composites/ImageCarousel'`.
 */

export { ImageCarousel } from './ImageCarousel'
