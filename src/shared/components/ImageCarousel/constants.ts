/**
 * Intervalos de autoplay del carrusel de imágenes (ms).
 *
 * @module shared/components/ImageCarousel/constants
 */

/** Intervalo entre avances automáticos del carrusel (ms). */
export const IMAGE_CAROUSEL_AUTOPLAY_MS: number = 4000

/**
 * Tras una navegación manual, no reanudar el autoplay hasta pasado este tiempo (ms).
 */
export const IMAGE_CAROUSEL_AUTOPLAY_PAUSE_AFTER_MANUAL_MS: number = 5500
