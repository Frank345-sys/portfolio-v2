/**
 * Barrel público de `TimelineItem`.
 *
 * Para datos de timeline (`TIMELINE_CHIP`, leyenda, etc.) importar desde
 * `@/shared/components/primitives/TimelineItem/constants`.
 *
 * @module shared/components/TimelineItem
 * @remarks Importar desde este barrel: `import { … } from '@/shared/components/primitives/TimelineItem'`.
 */
export { TimelineItem } from './TimelineItem'
export type { TimelineItemData } from './types'
