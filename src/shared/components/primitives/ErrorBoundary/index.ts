/**
 * Barrel de `ErrorBoundary`: re-exporta desde este directorio.
 *
 * @module shared/components/ErrorBoundary
 * @remarks Importar desde este barrel: `import { … } from '@/shared/components/primitives/ErrorBoundary'`.
 */

export { ErrorBoundary } from './ErrorBoundary'
