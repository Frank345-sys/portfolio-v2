/**
 * Barrel de `Modal`: re-exporta desde este directorio.
 *
 * @module shared/components/Modal
 * @remarks Importar desde este barrel: `import { … } from '@/shared/components/primitives/Modal'`.
 */

export { Modal } from './Modal'
