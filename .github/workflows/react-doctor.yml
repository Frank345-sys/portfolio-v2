name: React Doctor

on:
  pull_request:
    branches: ['main', 'develop']

concurrency:
  group: react-doctor-${{ github.repository }}-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  diagnose:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - run: npm ci

      - name: Run React Doctor (diff vs base branch)
        id: react-doctor
        env:
          BASE_REF: ${{ github.event.pull_request.base.ref }}
        continue-on-error: true
        run: |
          git fetch origin "${BASE_REF}"
          set +e
          npm exec react-doctor -- . --verbose -y --blocking error --diff "origin/${BASE_REF}" --pr-comment > react-doctor-report.txt 2>&1
          exit_code=$?
          set -e
          echo "exit_code=${exit_code}" >> "$GITHUB_OUTPUT"

      - name: Comment PR with report
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const report = fs.readFileSync('react-doctor-report.txt', 'utf8');
            const truncated = report.length > 60000
              ? report.substring(0, 60000) + '\n\n... (reporte truncado por longitud)'
              : report;
            const baseRef = process.env.BASE_REF ?? 'main';
            const body = [
              '## 🩺 React Doctor Report',
              '',
              `Modo **diff** frente a \`origin/${baseRef}\` (solo archivos del PR; evita falsos positivos de código muerto en todo el repo).`,
              '',
              '<details>',
              '<summary>Ver diagnóstico completo</summary>',
              '',
              '```',
              truncated,
              '```',
              '</details>',
              '',
              '> Generado automáticamente en cada PR hacia `main` o `develop`. Config: `doctor.config.ts` (`deadCode: false`). Auditoría completa local: `npm run react-doctor:full`.',
            ].join('\n');
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body,
            });
        env:
          BASE_REF: ${{ github.event.pull_request.base.ref }}

      - name: Fail if React Doctor reported issues
        if: steps.react-doctor.outputs.exit_code != '0'
        run: |
          echo "React Doctor encontró issues. Revisa el comentario del PR o ejecuta: npm run react-doctor -- --diff ${BASE_REF}"
          exit 1
        env:
          BASE_REF: ${{ github.event.pull_request.base.ref }}
